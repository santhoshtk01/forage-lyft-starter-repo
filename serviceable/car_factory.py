# TODO : Cars are created by calling the corresponding method on the `CarFactory`
from car import Car




class CarFactory:

    def create_calliope(self, current_date, last_service_date,
                        current_mileage: int, last_service_mileage: int) -> Car:
        # TODO: Create a car with capulet engine and spindler battery.
        pass

    def create_glissade(self, current_date, last_service_date,
                        current_mileage: int, last_service_mileage: int) -> Car:
        # TODO: Create a car with willoughby engine and spindler battery.
        pass

    def create_palindrom(self, current_date, last_service_date,
                         warning_light_on: bool) -> Car:
        # TODO: Create a car with strenman engine and spindler battery.
        pass

    def create_rorschach(self, current_date, last_service_date,
                         current_mileage: int, last_service_milage: int) -> Car:
        # TODO: Create a car with willoughby engine and nubbin battery.
        pass

    def create_thovex(self, current_date, last_service_date,
                      current_mileage: int, last_service_mileage: int) -> Car:
        # TODO: Create a car with capulet engine and nubbin battery.
        pass

